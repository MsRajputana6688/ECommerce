import Page from '../DataBase/NavData/Page'
import OurProdect from '../DataBase/NavData/OurProdect'
export const NavRedc = (state = Page, action) => {
    return state
}
export const NavCate = (state = OurProdect, action) => {
    return state
}